import React from 'react';
import ReactDOM from 'react-dom';
import * as SlideView from './SlideView.js';

ReactDOM.render(
  <React.StrictMode>
    <SlideView.Main />
  </React.StrictMode>,
  document.getElementById('root')
);
